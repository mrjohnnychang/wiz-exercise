name: IaC Security Pipeline
on: [push, pull_request]
jobs:
  scan-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Configure AWS
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: us-west-2

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3

      - name: Terraform Init
        run: terraform init

      - name: Run Checkov
        uses: bridgecrewio/checkov-action@master
        with:
          directory: .
          framework: terraform
          soft_fail: true #set to true to allow test deployment 

      - name: Terraform Plan
        env:
          TF_VAR_mongo_user: ${{ secrets.MONGO_USER }}
          TF_VAR_mongo_pass: ${{ secrets.MONGO_PASS }}
        run: terraform plan

      - name: Terraform Apply
        # Only run apply on pushes to main, not on pull requests
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        env:
          TF_VAR_mongo_user: ${{ secrets.MONGO_USER }}
          TF_VAR_mongo_pass: ${{ secrets.MONGO_PASS }}
        run: terraform apply -auto-approve
